"""Application core: configuration, domain model and events.

Everything under ``caissa.core`` is pure Python and free of heavy native
dependencies, so it imports in milliseconds and is type-checked under the full
``mypy --strict`` flag set (see ``pyproject.toml``).
"""

from __future__ import annotations

__all__: list[str] = []
